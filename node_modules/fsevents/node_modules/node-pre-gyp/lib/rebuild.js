"use strict";

module.exports = exports = rebuild;

exports.usage = 'Runs "clean" and "dist" at once';

function rebuild (gyp, argv, callback) {
  gyp.todo.unshift(
      { name: 'clean', args: [] },
      { name: 'dist', args: ['rebuild'] }
  );
  process.nextTick(callback);
}
